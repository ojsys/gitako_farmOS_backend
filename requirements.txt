Django==5.1.4
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.6.0
python-decouple==3.8
dj-database-url==2.3.0
whitenoise==6.8.2
psycopg[binary]==3.2.3
drf-spectacular==0.27.2

celery==5.4.0
redis==5.2.0
django-celery-beat==2.7.0

structlog==24.4.0
django-structlog==9.0.0

# No GIS dependency: locations are stored as plain lat/lng + GeoJSON, so the
# backend runs on standard PostgreSQL (no GDAL/PostGIS needed).

# Auth / security
argon2-cffi==23.1.0
pyotp==2.9.0

# Object storage
boto3==1.35.71

# Dev / test
pytest==8.3.4
pytest-django==4.9.0
pytest-asyncio==0.24.0
factory-boy==3.3.1
freezegun==1.5.1
ruff==0.8.4

# Misc
requests==2.32.3
sentry-sdk==2.19.2

# AI co-pilot (M12) — only needed when LLM_PROVIDER=claude; the stub provider
# runs without it. Lazy-imported by apps.copilot.providers.
anthropic==0.69.0
